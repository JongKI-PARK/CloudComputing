spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_process?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: sys123457!

  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: false

server:
  port: 8085
