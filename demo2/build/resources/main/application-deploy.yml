spring:
  datasource:
    url: jdbc:mysql://mysql-container:3306/order_process?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: false

server:
  port: 8085
